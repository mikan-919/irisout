## 1. render.ts: ネスト検出の再帰化

- [x] 1.1 現状の「構造ユニット本体の中に構造ユニットを見つけたら即
      `scope limit`」判定を、ネスト深さカウンタ付きの再帰呼び出しに置き換える。
- [x] 1.2 深さ1(トップレベル直下)は既存どおり無制限に受理、深さ2
      (1階層ネスト)は受理、深さ3以降は`compile: nested structural unit
      exceeds 1 level of nesting is not supported yet (scope limit)`で拒否する。
- [x] 1.3 リストアイテム本体・条件分岐ブランチ本体それぞれについて、
      「兄弟要素との混在は不可」(既存M5の制約)がネストした構造ユニットにも
      同様に適用されることを確認し、必要ならエラーメッセージを揃える。

## 2. analyze.ts: 識別子解決・依存解析の再帰化

- [x] 2.1 ネストした構造ユニット内の識別子解決(declIdへの解決)が、外側の
      構造ユニットのローカル変数(例: リストアイテムの`todo`フィールド)を
      正しくスコープチェーンとして辿れるようにする。
- [x] 2.2 ネストした構造ユニットの条件式・依存収集を、既存のトップレベル
      向けロジックに対して再帰的に適用する。

## 3. codegen.ts: ネストしたfactory/update関数の生成

- [x] 3.1 `generateFactory()`が、生成対象の本体にネストした構造ユニットの
      マーカーを見つけたら、そのfactory関数のクロージャの中に入れ子の
      `update_*`/`__create_*__`を生成するようにする(design.md Decision 3)。
- [x] 3.2 `generateConditionalUpdate()`のネスト版・リストのkeyed diff生成の
      ネスト版が、外側factory関数のローカルスコープ内で正しく動作することを
      確認する(モジュール直下の`__markers__`等に依存しない)。
- [x] 3.3 ネストした構造ユニットについても、外側が破棄されるときに明示的な
      teardownを生成しない(design.md Decision 1、ADR-0005決定4を踏襲)。

## 4. フィクスチャの更新

- [x] 4.1 `examples/todomvc.jsx`のUNRESOLVED(06)を解消し、
      `{visibleTodos().length > 0 && (<ul>...</ul>)}`が実際にコンパイル
      できることを確認する。
- [x] 4.2 `examples/todomvc.jsx`のUNRESOLVED(07)を解消し、リストアイテム内の
      編集モード切り替え(span/input)が実際にコンパイルできることを確認する。
- [x] 4.3 UNRESOLVED(04)(アイテムごとのローカル編集状態)・(08)(編集中
      テキストの下書き保持先)を、07の実装で自然に解消するか判断する。
      解消するならフィクスチャのコメントを更新、解消しないなら理由を明記して
      スコープ外のままにする。
- [x] 4.4 `examples/todomvc.handwritten.js`の06/07コメント(妥協の説明)を、
      「生成器側は実DOM着脱で解決した」旨に更新するか、handwritten側は
      意図的な人力実装の参考のまま残すかを判断し、コメントを整合させる。

## 5. テスト

- [x] 5.1 条件分岐ブランチ内にリストがネストするケースのテストを追加する
      (`test/*.test.ts`、`loadGenerated()`で実際に実行して検証)。
- [x] 5.2 リストアイテム内に条件分岐がネストするケースのテストを追加する。
- [x] 5.3 2階層以上のネストが`scope limit`エラーになることを踏むテストを
      追加する(コーディング規約: 新しいscope limitにはそれを踏むテストを足す)。
- [x] 5.4 「外側条件分岐によるリスト全体の非マウント」時に内側リストの
      keyed Mapが全件破棄される境界条件のテストを追加する(specs参照)。
- [x] 5.5 `bun run check-all`(biome → tsc --noEmit → bun test)を通す。

## 6. ドキュメント更新

- [x] 6.1 `STATUS.md`のM5.5行を`TODO`から`DONE`に更新し、備考にchange名を書く。
- [x] 6.2 `STATUS.md`の「既知の制約」から、06/07に関するscope limitの記述を
      削除し、新しい制約(2階層以上のネスト拒否、フィルタ全件除外時の
      状態破棄)に置き換える。
- [x] 6.3 `ROADMAP.md`の「次のアクション」5番を実績(完了・change名)に更新する。
