## 1. 入力と解析

- [ ] 1.1 文書メタデータと8分類を登録し、重複slug・順序重複がビルドエラーになることを確認する
- [ ] 1.2 `docs/getting-started.md`を正本として読み込み、本文の複製なしにHTMLを生成できることを確認する
- [ ] 1.3 任意HTML、未知のコード言語、切れた文書・見出し・画像・例参照を検査して失敗することを確認する

## 2. HTMLと索引

- [ ] 2.1 文書一覧・詳細、目次、前後リンク、正規URLを生成し、JavaScriptなしの直接URLで読めることを確認する
- [ ] 2.2 タイトルと見出しの検索索引を生成し、文書ページがMarkdown解析器を読み込まないことを依存一覧で確認する
- [ ] 2.3 Counter・List・SVGの表示用コードとPlayground入力が同じ例入力から生成され、`irisout@0.2.2`で検査できることを確認する

## 3. 検査接続

- [ ] 3.1 `build:web`とサイト検査をCIの実行入口へ追加し、`bun run build:web`が成功することを確認する
- [ ] 3.2 `bun run check`、`bun run test`、`bun run build:web`とOpenSpec検証が成功することを確認する
