## 1. 入力の整理

- [x] 1.1 `examples/todomvc.jsx` の `UNRESOLVED` コメントと `ROADMAP.md` の
      対応表から、ハンドラ関連の項目(特に (09) イベント引数の型付け)を
      抽出する。
- [x] 1.2 `src/compiler/render.ts`・`src/compiler/analyze.ts`・
      `src/codegen.ts` の該当箇所(ハンドラのブロック本体/引数拒否、
      `analyzeHandlerExpr`、ハンドララッパーの `(...__args)`)を実コードで
      確認し、ADR に引用する行を確定する。

## 2. スクラッチ実験(非コミット)

- [x] 2.1 リポジトリ外のスクラッチ領域で `@babel/parser` +
      `@babel/traverse` を使い、ブロック本体内のローカル変数宣言に対して
      `resolveDeclId` の `getBinding()` が追跡対象外(`null`)を返すことを
      確認する。
- [x] 2.2 ハンドラ引数名・ローカル変数名が追跡中の signal 名と衝突する
      シャドーイングのケースで、`getBinding()` がローカル側へ正しく解決
      することを確認する。
- [x] 2.3 `if (...) { count(1); return; }` のような早期 return・条件分岐内
      の書き込みを、traverse ベースの走査が正しく拾うことを確認する。
- [x] 2.4 3項目それぞれの確認結果(確認できた/できなかった)と根拠となる
      最小コード片をメモに残す。スクラッチファイルはリポジトリにコミット
      しない。

## 3. ADR ドラフトの執筆

- [x] 3.1 `docs/adr/0009-handler-statements-and-event-object.md` を新規
      作成し、ステータスを「ドラフト(レビュー待ち)」とする。既存 ADR
      (0001〜0008)と同じ見出し構成(ステータス/コンテキスト/決定(ここでは
      提案)/受け入れた代償/未決定事項)に従う。
- [x] 3.2 5つの設計質問(イベント引数の受け渡し、ブロック本体の最小文種、
      分岐内書き込み検出の意味論、`analyzeHandlerExpr` の拡張形、
      ADR-0008 との接続)それぞれに推奨案・根拠・検討した代案を書く。
- [x] 3.3 Step 2 のスクラッチ実験結果を、該当する設計質問の根拠として
      文章と最小コード片で引用する。
- [x] 3.4 各設計質問について、`examples/todomvc.jsx` の該当
      `UNRESOLVED` 実例をコンテキスト節で参照する。

## 4. ROADMAP.md の更新と最終確認

- [x] 4.1 `ROADMAP.md` の該当項目(ハンドラ関連の未規定 API)を
      「ADR-0009 ドラフトあり・レビュー待ち」へ状態付け替えする(削除では
      なく参照追加)。
- [x] 4.2 `bun run check-all` を実行し、既存テストが全てパスすることを
      確認する(何も壊していないことの確認)。
- [x] 4.3 `git status` で変更が `docs/adr/0009-*.md` と `ROADMAP.md` のみで
      あることを確認する(`src/**`・`test/**` に差分がないこと)。
