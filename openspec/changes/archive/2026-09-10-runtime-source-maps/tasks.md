## 1. 位置情報の保持

- [x] 1.1 Source Map v3生成ライブラリをコンパイラの直接依存へ追加し、梱包物から解決できることを`bun run pack:smoke`で確認する
- [x] 1.2 解析結果へ生成断片内の位置と連結ソース位置の対応を追加し、状態読み書きを書き換えた処理文の単体試験で確認する
- [x] 1.3 専用生成器へ位置付き断片の出力処理を追加し、既存の生成JavaScriptのスナップショットが変わらないことを確認する

## 2. イベント処理の最初の対応

- [x] 2.1 ルートイベント処理の生成位置を`<source>`の処理文へ対応付け、同じ文が複数ある場合を含む地図照会試験を通す
- [x] 2.2 `compileProject()`の連結範囲を使って別ファイル部品の処理文へ対応付け、入口ファイルへ誤対応しない試験を通す
- [x] 2.3 `await`より前後の非同期イベント処理を元の各処理文へ対応付ける試験を通す

## 3. アクションとライフサイクル

- [x] 3.1 `use=`の初期化、更新、`destroy`に由来する生成位置を元の処理文へ対応付け、構造単位の試験を通す
- [x] 3.2 `onMount`の初期化と片付け処理を元の処理文へ対応付け、ルートと構造単位の試験を通す
- [x] 3.3 `effect`の初回実行、再実行、片付け処理を元の処理文へ対応付け、ルートと構造単位の試験を通す

## 4. Vite連携と回帰確認

- [x] 4.1 Vite仮想モジュールの読込結果へコードと地図を返し、自動`hydrate`文が元JSXへ誤対応しない連携試験を通す
- [x] 4.2 `build.sourcemap: true`の別アプリを本番ビルドし、出力地図からイベント処理の元ファイル、行、列を確認する
- [x] 4.3 `bun run check`、`bun run test`、`bun run build`、`bun run build:packages`、`bun run pack:smoke`を通し、生成サイズと動作に退行がないことを確認する
- [x] 4.4 対応範囲と未対応の生成処理をREADME、STATUS、ADR-0049へ反映し、OpenSpec変更を厳密検証する
