## 1. 隔離配信

- [x] 1.1 公式サイトと実行管理画面を別配信元へ配置し、管理鍵・保存APIのCookieが渡らないことを確認する
- [x] 1.2 実行管理iframe、Worker、結果iframeのCSPと応答ヘッダーを定義し、ブラウザ通信で確認する
- [x] 1.3 結果iframeのsandbox権限を設定し、親DOM、フォーム、上位遷移、ポップアップ、ダウンロードが拒否されることを確認する

## 2. 実行状態

- [x] 2.1 実行ごとのWorker生成・成功・失敗・取消し・破棄を接続し、5秒超過で停止することを確認する
- [x] 2.2 結果サイズ1 MiB超過を診断として返し、旧実行番号の応答を表示しないことを確認する
- [x] 2.3 メッセージの送信元、形式、サイズ、実行番号を検証し、受信値を親画面でHTMLとして解釈しないことを確認する

## 3. 復旧検証

- [x] 3.1 無限ループ、例外、外部通信、結果画面の遷移、連続実行、停止後の再実行をブラウザで確認する
- [x] 3.2 操作不能が残らないことと残る制約を記録し、成立しない場合は公開を止める判定を確認する
- [x] 3.3 `bun run check`、`bun run test`、`bun run build:web`とOpenSpec検証が成功することを確認する
