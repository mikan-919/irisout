## 1. 保存形式とAPI

- [x] 1.1 `schemaVersion`、タイトル、説明、単一JSX、版、`unlisted`、requestIdの入力検証を実装し、不正型・未知項目を拒否することを確認する
- [x] 1.2 128ビット以上の新規ID、作成時刻、管理鍵ハッシュ、削除状態をサーバー側だけで設定することを確認する
- [x] 1.3 本文128 KiB、ソース64 KiB、タイトル120、説明1000の上限を日本語の文字数とバイト数で確認する

## 2. 再送と削除

- [x] 2.1 同一requestId・管理鍵・入力の再送が同じIDを返し、異なる入力が409になることを確認する
- [x] 2.2 管理鍵で削除した共有が404になり、削除済み再送で復活せず、無権限DELETEが404になることを確認する
- [x] 2.3 通信断、再起動、保存先停止、429、503で入力を失わず再試行できることを確認する

## 3. 投稿コード境界

- [x] 3.1 保存・閲覧・削除・OGP生成の各経路で投稿JSXがコンパイル・実行されないことを副作用入力で確認する
- [x] 3.2 管理鍵がHTML、OGP、検索索引、ログ、読取りデータへ出ないことを確認する
- [x] 3.3 `bun run check`、`bun run test`とOpenSpec検証が成功することを確認する
