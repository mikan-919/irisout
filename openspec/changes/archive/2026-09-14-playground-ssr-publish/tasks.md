## 1. 要求描画

- [x] 1.1 ビルド後に作成した共有IDを再ビルドなしで読み込み、200と初期HTMLを返すことを確認する
- [x] 1.2 タイトル、説明、ソース全文、版、作成日時、未実行表示を同じ表示データからhead・本文・stateへ出力することを確認する
- [x] 1.3 未存在、削除済み、形式不正のIDで404となり、正常ページ用stateを返さないことを確認する

## 2. hydrateと安全な出力

- [x] 2.1 JavaScript無効時の本文と有効時のhydrate後DOMのソース、版、見出し、文字数が一致することを確認する
- [x] 2.2 閉じタグ、引用符、`</script>`、イベント属性を含む値をHTMLとstateへ安全に出力できることを確認する
- [x] 2.3 管理鍵、削除用値、投稿JSXの実行結果がHTML、state、OGP、ログへ出ないことを確認する

## 3. 障害と公開前検証

- [x] 3.1 A/Bの並行要求、片方の失敗、直後の正常要求でstateが混ざらないことを確認する
- [x] 3.2 保存先停止時に503を返し、`no-store`、`noindex`、`no-referrer`を設定することを確認する
- [x] 3.3 バックアップ復元後に削除記録を再適用し、`bun run check`、`bun run test`、`bun run build`とOpenSpec検証が成功することを確認する
