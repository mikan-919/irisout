{
  "name": "irisout",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test --path-ignore-patterns='legacy/**'",
    "build": "bun scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "check-all": "biome check --write && bun run typecheck && bun run test",
    "prepare": "husky"
  },
  "dependencies": {
    "@babel/parser": "^8.0.0",
    "@babel/traverse": "^8.0.0"
  },
  "devDependencies": {
    "@babel/types": "^8.0.0",
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.14",
    "@types/jsdom": "^28.0.3",
    "husky": "^9.1.7",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.0"
  }
}
