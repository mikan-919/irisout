# irisout v2 コンセプト

## 目的

irisout は React の代替ではありません。

その目的は、JSX の宣言的な開発体験を保ちながら、経験豊富なフロントエンドエンジニアが DOM API を使って手書きするのと同じ JavaScript を生成することです。

ブラウザに届くのは、静的 HTML と専用の更新コードだけであるべきです。汎用ランタイム、仮想 DOM、リアクティブエンジンは、コンパイル時に消え去るべきです。

---

## 設計原則

* 可能な限り HTML を生成する。
* 可能な限りランタイムの抽象化を排除する。
* コンポーネント境界はビルド後に消えるべきである。
* 更新は、影響を受ける DOM ノードを直接操作するべきである。
* すべての最適化は、次の問いに答えるべきである。

> 「もし人間がこの UI を Vanilla JavaScript で実装するなら、どう書くか？」

### authoring API を貫く3原則(ADR-0011)

* **穴のない宣言**: 「あとで埋まる箱」は存在しない。UI より前には完成した
  値だけが置かれ(const/signal — 生まれた瞬間から値を持つ)、UI より後には
  中身の供給だけが置かれる(function 宣言 — UI が宣言した名前に差し込まれる)。
  `render()` がその境界線である。
* **プレースホルダーの向き**: 名前は UI が宣言し、後段が差し込む。
  `{count()}`(テキスト穴←signal)、`onClick={handle}`(動きの穴←function
  宣言)、`use={setup}`(振る舞いの穴←function 宣言)はすべて同じ向き。
  逆流(UI より前に宣言した箱への裏からの注入)はない。
* **位置的リアクティビティ**: リアクティブ性は「どう読むか」ではなく
  「どこに書いたか」でコンパイル時に決まる。購読という実行時概念は存在
  しない。リアクティブな置き場所は JSX の穴・derived 本体・action の
  返り値クロージャのみで、それ以外での signal 読みは常に「その瞬間の
  現在値」である。

---

## ビルド戦略

irisout は主にビルド時システムです。

コンポーネント定義はビルド時に実行され、アプリケーションのリアクティブグラフを構築します。

JavaScript の実行は、それが自然に情報を提供できる箇所（コンポーネント定義、状態登録、props、ハンドラなど）で用います。

静的解析は、実行だけでは本質的に情報を失ってしまう箇所（主に JSX 式）にのみ用います。

目的は、**完全な JavaScript コンパイラを作ることではありません。**

その代わりに irisout は、最適な DOM 更新コードを生成するために必要な最小限の情報だけを抽出します。

---

## 責務

### ランタイム実行

実行は、次の情報を発見する役割を担います。

* コンポーネント
* シグナル
* 派生値
* props
* ハンドラ
* コンポーネント階層
* ビルド時値

### 静的解析

静的解析は、次の情報を発見する役割を担います。

* JSX の構造
* JSX 内の式
* それらの式の依存関係

例:

```tsx
<span>{x + 2}{y}</span>
```

は、概念的には次のようになります。

```text
Element(span)

Expression
    code: x + 2
    deps: [x]

Expression
    code: y
    deps: [y]
```

irisout が関心を持つのは依存関係の情報であり、任意の JavaScript を解釈することではありません。

---

## リアクティブグラフ

実行と静的解析は、最終的に 1 つの依存グラフに集約されます。

例:

```text
count
   │
   ▼
double
   │
   ▼
TextNode#3
```

または

```text
count
 ├───────────────┐
 ▼               ▼
Text#1      Attribute#5
```

この依存グラフが、コード生成の単一の真実の源になります。

---

## コード生成

グラフは、専用の更新関数へ直接コンパイルされます。

例:

```js
let count = 0;

function update_count() {
    text.data = count;
    title.textContent = count;
}
```

ブラウザ内に汎用的なオブザーバーシステムは存在しません。

生成される関数は、すべて 1 つのアプリケーション専用です。

---

## 哲学

irisout は、大規模なランタイムシステムを意図的に避けます。

また、完全な JavaScript コンパイラになることも避けます。

その代わりに、次のものを組み合わせます。

* 宣言的な構造のためのビルド時実行
* 実行だけでは回収できない情報のための最小限の静的解析
* 各アプリケーションごとの専用コード生成

理想的な出力はフレームワークコードではありません。

理想的な出力は、経験豊富な JavaScript 開発者が手作業で書いたであろうコードです。
