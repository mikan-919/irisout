# irisout ロードマップ

更新日: 2026-09-10。方針と判断の根拠は[開発方針](./docs/project-direction.md)に記載する。
この文書は今後の作業と完了条件を扱う。実装状況は[STATUS.md](./STATUS.md)、従来の計画と完了記録は[旧ロードマップ](./docs/history/roadmap-through-2026-09-07.md)を参照する。

## 到達目標と順序

作者以外の開発者が、別のリポジトリでirisoutを導入し、ブラウザ内ツールを実装・検証できる試用版を目指す。ヒートマップを利用例として継続し、記述上の制約、生成物の性能、外部導入を検証する。

| 順序 | 段階                                             | 得る結果                                 | 依存する段階                   |
| ---- | ------------------------------------------------ | ---------------------------------------- | ------------------------------ |
| 1    | R1: 対応範囲と診断を揃える                       | 利用者が記述の可否と失敗箇所を判断できる | なし                           |
| 2    | R2: ヒートマップで部品分割と非同期更新を検証する | 段落解析から表示までを分割して記述できる | R1                             |
| 3    | R3: 初期化処理を改善する                         | 原因と効果を記録した性能改善             | 基準計測はR1後、最終検証はR2後 |
| 4    | R4: 外部導入と試用版の配布準備を整える           | 配布物だけで動く導入例と試用結果         | 準備はR1後、完了判定はR2・R3後 |

開発人数と稼働時間が未定のため、日付による期限は設けない。各段階を一つの変更にまとめず、以下の作業単位で進める。実装量が増える場合も、完了条件を変更する前に利用例と原因を確認する。

## 現在の進捗(2026-09-09)

- R1: 相対moduleをリンクした診断と、イベント処理、`use=`、`onMount`、`effect`の実行時Source Mapを実装した。共有実行時処理など、一対一の元構文がない生成処理は対応対象外である。
- R2: ヒートマップを部品とWorkerへ分割し、段落ごとの値・理由、指標切り替え、入力上限、要求競合、文字変換中、破棄を実装した。ブラウザ性能基準は既存測定を再確認する。
- R3: 初回Listの一括DOM挿入、初回経路分岐、item markerの要素走査、item handlerのcallback共有に加え、条件付き初期枝の限定引き取りを実装した。同一条件の最終比較は14.5%短縮だった。20%の仮目標は実測とイベント契約に基づいて改訂し、現行実装を採用してR3を完了した。詳細はADR-0045とADR-0046へ記録した。
- R4: コンパイラ、実行時処理、Vite連携、JSX型定義を単一の`irisout` 0.1.0へまとめ、npmへ公開した。workspace外で型検査とVite buildを行う梱包物検査とnpm公開版検査を用意した。三つのサブエージェントによる隔離試用で導入、アプリ作成、診断修正を確認した。人間による手動試用は未実施である。

## R1: 対応範囲と診断を揃える

目的は、型検査・コンパイル・実行で保証する範囲と、失敗箇所の調べ方を一致させることである。

### 作業順序

1. `compile()`と`compileProject()`、相対モジュールと外部ライブラリ、部品入力とイベント、同期・非同期更新について、受理例・拒否例・代替記述を一覧にする。既存仕様と試験を根拠に、文書間の古い制約表記を修正する。
2. 別ファイルの子部品で起こす構文エラー、未対応構文、型誤りを確認する。診断が入口や別の行を指す場合は再現試験を追加し、元ファイルの位置を保持する経路を修正する。
3. 初期表示、イベント、Promise完了、条件分岐の切り替え、破棄後の完了通知について既存試験を照合し、未検証の組み合わせを埋める。更新漏れや破棄漏れがあれば先に修正する。
4. 生成コード中のイベント処理と例外発生箇所を元のJSXまで辿る例を作る。対応する位置情報を保持し、ソースマップの最初の対象範囲を決めて実装する。

### 完了条件

- 対応表の各項目から仕様または試験へ辿れ、型が通ってもコンパイルできない例が説明されている。
- 別ファイルの失敗を、そのファイルと原因となる行・列から修正できる。位置を特定できない診断は、その限界が分かる表示になっている。
- 代表イベントの実行時例外を元のJSXへ辿れる。未対応の生成処理については、調査方法と制約が記載されている。
- 通常の検査と試験を通過し、確認した状態更新・破棄の経路に未修正の動作不良がない。

注: 型検査は値と関数の使い方を検査し、コンパイル時の構文制限は静的に更新処理へ変換できるかを検査する。両者が同じ範囲を保証するとは限らない。

## R2: ヒートマップで部品分割と非同期更新を検証する

目的は、入力、解析、一覧、選択、詳細表示を分割し、コンパイラの制約を確認できる利用例にすることである。

### 作業順序

1. 画面の部品、指標の計算、Workerとの通信を分割する。要素の取得は`use=`で受け取る要素など、所有する画面の範囲へ限定する。同じアプリを二つ配置して、状態・イベント・破棄が混ざらないことを確認する。
2. 段落ごとの用語出現など、一つの解析指標を選び、入力例と期待値を定める。Workerから段落識別子、表示値、算出理由を返し、色・数値・詳細表示へ接続する。文字列の長さや空段落による偏りを記載する。
3. 連続入力、文字変換中、古い結果、解析失敗、上限超過、画面破棄を操作試験へ含める。要求の待ち行列が最新入力の表示を遅らせる場合は、未処理要求を最新の一件へまとめる処理をアプリ側で検証する。
4. 分割によって必要になった記述が拒否される場合は、例を独立した試験へ落とし込み、既存記法の改善か構文の拡張かを決める。propsや非同期構文を一括して拡張しない。

### 完了条件

- 本文入力から段落ごとの解析結果、指標切り替え、色付け、選択段落への移動が連動する。原文と色の理由を確認できる。
- 複数ファイルと二つの配置で動作し、片方の入力や破棄が他方へ影響しない。
- 文字確定前の解析要求を抑止し、古い結果を表示せず、失敗と上限超過を表示する。色以外の数値とキーボードでも操作できる。
- 同じ測定条件の300段落・25,000文字以内で、入力から解析完了表示1,000ms以下、ページ側ヒープ増分32MiB以下、段落移動16ms以下を再確認する。従来計測の「入力から表示までの時間とWorker解析時間の差」100ms以下も確認し、純粋な描画時間とは区別する。
- 指標追加で基準を満たさなくなった場合は、解析、要求待ち、表示更新のどこが原因かを記録し、処理を見直すか対応範囲を明示して改訂してから完了とする。

注: Workerは画面操作を担当する処理とは別にJavaScriptを実行するブラウザ機能である。古い結果を捨てても、その解析の実行時間は残るため、要求の蓄積は別に確認する。「情報量」は文章の特徴の呼称であり、重要性や正しさの判定ではない。

## R3: 初期化処理を改善する

目的は、生成版の一覧初期化に必要な処理と、重複・再探索による費用を分けて改善することである。

### 作業順序

1. 現行版でTodoMVCの100・1,000・10,000件とヒートマップを再計測する。コミット、環境、入力、反復数を残し、既存の初期化区間に加え、初期HTMLの読み込みから操作可能になるまでの区間を分けて測る。
2. DOM範囲の収集、テンプレート生成、部品ごとの生成処理、要素探索、イベント登録の時間と回数を調べる。寄与が確認できた処理を一つ選び、修正前後を比較する。
3. 未使用機能の出力除外と共有処理の選択を確認する。イベントの意味、一覧の同一キーの再利用、状態更新、初期化と破棄を維持する。
4. 決定的に比較できる生成物のサイズと操作回数を自動検査へ追加する。時間・メモリは環境と反復測定を揃えて記録する。

### 完了条件

- 基準測定と原因分析があり、採用した変更の効果を再現できる。
- 同一環境の現行基準に対し、1万件の初期化中央値に測定のばらつきを超える短縮があり、原因と再現方法を記録する。20%の固定目標はADR-0046で改訂し、現行実装の14.5%短縮を採用する。
- 100・1,000件、通常更新、転送量、メモリも比較する。測定のばらつきを超える悪化がある場合は、理由と採否を設計判断へ記録する。
- counterの既存サイズ上限を守り、未使用機能を含めない試験とR2の操作・性能基準を通過する。

短縮率だけで採否を決めず、イベント契約、対応範囲、通常更新、転送量、メモリとの交換条件を記録する。転送量だけから初期化速度を推定せず、手書き版やReactの値をコンパイラの短縮率として扱わない。

## R4: 外部導入と試用版の配布準備を整える

目的は、開発リポジトリの配置や依存解決に頼らず、配布物からアプリを作れることを確認することである。

### 作業順序

1. コンパイラ、実行時処理、Vite連携の公開入口と内部入口を区別し、対応する実行環境、Viteとの組み合わせ、版番号、変更履歴の規則を決める。通常のViteでの動作は試験するまで保証へ含めない。
2. 配布用JavaScript、型定義、ライセンス、必要な依存を梱包する。リポジトリ内専用の依存指定とソースの絶対パスが残らないことを確認する。
3. 作業ディレクトリ外の一時ディレクトリへ梱包物を導入し、型検査、開発時の変更反映、本番ビルド、初期表示、イベント更新、破棄を検証する。親リポジトリの依存関係がなくても実行できる条件で試験する。
4. 入力・一覧・非同期処理を含む導入手順と、対応ブラウザでの操作試験を用意する。最初にChromiumを基準とし、Firefox・WebKitは検証結果に応じて対応表へ記載する。
5. 作者以外の開発者による試用を行い、導入、部品分割、エラー修正で止まった箇所を記録する。試用協力が未実施の場合は、外部試用待ちとして残す。

### 完了条件

- 梱包物だけを使う別リポジトリ相当の導入試験が通る。
- 公開入口、対応範囲、既知の制約、移行方法、再現に必要な情報の報告手順が文書化されている。
- 作者以外の協力者がいる場合は、少なくとも一人が手順に沿って導入・編集・ビルドし、開発を止める問題を解消するか制約として説明できている。協力者がいない場合は、公開文書とnpm公開版だけを使う隔離試用を行い、人間の試用が未実施であることを残す。自動試験の成功だけを試用の代用にはしない。
- R1〜R3の完了条件と配布物の確認結果を揃え、試用版として公開するか判断できる。

注: 梱包物とは、利用者へ渡すファイルをまとめたものを指す。現在は`bun pm pack`で
単一の`irisout` tarballを作り、`bun run pack:smoke`でworkspace外の導入を検査する。0.1.1は
2026-09-10にnpmへ公開し、公開版を指定した隔離導入検査を通した。次版では状態更新を
`signal((previous) => next)`へ統一し、互換性を壊すため0.2.1として準備する。作者以外の試用者への
連絡はこの計画では決めない。

## 次に着手する変更

0.2.1では`signal(initial, keyOf)`と`.update()`を削除し、配列も関数形式のsignal更新へ統一する。
ローカル梱包物で型検査、一覧更新、本番ビルドを確認してから公開を判断する。公開済み版を固定した
`registry:smoke`は0.1.1を使う。続く変更は、人間の協力者が得られた場合の手動導入試用、
または公開後に報告された再現可能な不具合を扱う。共有実行時処理と
自動生成した更新関数は、一対一の元構文がないためソースマップの対応対象へ広げない。
範囲と理由はADR-0049、版更新の判断はADR-0050に記録する。
人間による手動導入試用は協力者を得られた場合に行い、導入、部品分割、診断修正、イベント更新、
破棄を確認する。各変更では設計判断を
[ADR](./docs/adr/)へ、実装の受け入れ条件を[仕様](./openspec/specs/)へ記録する。通常の完了確認は
`bun run check`、`bun run test`、`bun run build`、`bun run build:packages`、`bun run pack:smoke`
とし、DOMや非同期処理を変える場合は該当するブラウザ操作試験、性能を変える場合は比較測定を行う。

段階完了時は[STATUS.md](./STATUS.md)を更新し、この文書には次の作業と未決事項だけを残す。期限・配布日・安定版の互換性保証は、各段階の実測と外部試用の結果をもとに決める。
