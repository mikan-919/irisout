# irisout ロードマップ

更新日: 2026-09-15。方針と判断の根拠は[開発方針](./docs/project-direction.md)に記載する。
この文書は今後の作業と着手条件だけを扱う。実装状況は[STATUS.md](./STATUS.md)、設計判断は
[ADR](./docs/adr/)、受入条件は[OpenSpec](./openspec/specs/)を参照する。

## 今後の作業

1. 実運用の公式Origin、実行管理Origin、保存先、復元手順を固定する。
2. 実運用相当のブラウザー検査を各配信環境で行う。
3. 作者以外の利用者による導入、部品分割、診断修正、イベント更新、破棄を試す。
4. 試用で再現した不足だけを小さな仕様変更として追加する。

irisout本体の機能追加は、次のいずれかを確認した場合に着手する。

1. 再現可能な不具合
2. 既存の記法では表せない実際の利用例
3. 計測で確認した速度、転送量、メモリの問題
4. 導入手順、診断、対応範囲の説明不足
5. 依存関係または対応環境の変更

人間の協力者が得られた場合は、導入、部品分割、診断修正、イベント更新、破棄を確認し、
止まった箇所を再現例として記録する。協力者を前提に期限は設けない。

公式サイトと共有Playgroundの契約は`openspec/specs/`、設計判断はADR-0052〜0054に記録する。

公開一覧、認証付き非公開、Suspense、Transition、Portal、部品単位の例外回復は予定へ置かない。
必要性を示す利用例と、既存記法またはブラウザ標準機能では不足する理由が得られた場合に検討する。

## 変更時の確認

通常は`bun run check`、`bun run test`、`bun run build`、`bun run build:packages`、
`bun run pack:smoke`を実行する。DOMや非同期処理を変える場合は該当するブラウザ操作試験、
性能を変える場合は同一条件の比較測定を行う。仕様変更は`openspec/specs/`、設計判断は
`docs/adr/`、状態の変化は`STATUS.md`へ記録する。
