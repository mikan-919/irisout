# C1

C1-T1 [R1,K1,K2] Monacoの入力モデルと言語定義をTypeScript/TSX向けにし、既存の遅延読込み、TypeScript作業スレッド、意味検査を無効にした設定、テーマ、読取り専用状態、アクセシビリティ属性を保ったまま、型構文とJavaScript/JSXの構文要素を色分けする。
done: 初期TSXソースを表示したとき、型注釈、型別名、interface、ジェネリクス、キーワード、コメント、文字列、JSXタグ、属性、式が通常のJSX本文と異なる構文色になる。補完・型検査・診断は有効にならない。

C1-T2 [A1] <- C1-T1 -> 初期TSXソースの表示を実ブラウザー試験で確認する。
done: Chromium上のMonacoトークンにC1-T1の各構文要素が現れ、代表的な構文色が通常テキストの色と一致しない。

# C2

C2-T1 [R2,R3,K2,K3,A2] <- C1 -> ソースの編集、公式例の切替え、共有ページからの複製が同じ値経路を通り、置換後もTypeScript/TSX言語定義、構文ハイライト、読取り専用状態を維持する。
done: Monacoの編集値と`textarea`の値が各経路で一致し、保存、管理鍵付き共有、書き出し、実行へ渡るsourceがその値と一致する。単一ソース、64 KiB制限、保存形式、コンパイラ版は変わらない。

C2-T2 [R4,K3,A3] <- C1 -> Monaco本体または言語機能の遅延読込み失敗時に、既存の`textarea`へ安全に退避する。
done: 読込み失敗を再現したChromium試験で初期値・複製値・利用者入力が保持され、`textarea`が表示され、入力、保存、書き出しを継続できる。

C2-T3 [R5,K4,A4] <- C2-T1,C2-T2 -> TypeScript/TSX表示の変更が実行隔離と送信元境界へ影響しないことを回帰確認する。
done: 実行管理画面、Worker、結果iframeの送信元検査・CSP・sandbox・権限制限、投稿sourceのサーバー変換禁止が既存試験で維持され、`bun run typecheck:web` と `bun run test:web:playground`が成功する。型消去・変換は実行経路へ追加されない。
