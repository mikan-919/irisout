# C1

C1-T1 ファイル経路の規約、照合結果、衝突診断、再生成時の振る舞いをOpenSpecの仕様変更として定義する [R1,R2,R6,R7,R8,R14]
done: `page.jsx`の配置例、静的経路の優先、動的経路の衝突、末尾斜線、引数の復号、未一致、ファイル変更後の結果について受入条件があり、このまとまりの実装判断に必要な振る舞いを確認できる。

C1-T2 指定ディレクトリを走査して`page.jsx`だけから共通の経路定義を生成し、競合する定義を対象ファイル付きで拒否する [R1,R2,R6,R8] <- C1-T1
done: ルート、静的階層、動的階層を期待する経路定義へ変換でき、同順位の衝突は両方のファイルを示して生成に失敗し、追加・削除・名前変更後の再生成には現在のファイル集合だけが現れる。

C1-T3 共通の経路定義から環境非依存の経路表と照合処理を生成し、サーバー側とブラウザー側で同じ解決結果を得られるようにする [R2,R5,R7,R14] <- C1-T2
done: 静的経路が動的経路より優先され、末尾斜線、検索引数、断片、符号化された引数、不正な符号化、復号後の経路区切り、未一致に対する両環境の結果が一致し、ブラウザー用生成物がファイルシステムとHonoに依存しない。

C1-T4 ファイル集合の変更を含む経路生成と照合の受入条件を自動検証する [R1,R2,R5,R6,R7,R8,R14] <- C1-T3
done: 同じファイル集合から得た生成物の一致、静的優先、衝突診断、追加・削除・名前変更、未一致、経路の正規化と復号、ブラウザー用依存の検査が自動試験で再現できる。

# C2

C2-T1 `irisout/hono`の公開境界と、ページ、ブラウザー用生成物、取得処理を対応付けるサーバー側の契約を定める [R1,R3,R4,R11,R12,R13] <- C1-T4
done: 単一の`irisout`パッケージから副入口を利用でき、接頭辞付きサブルーターへ経路定義を渡せる公開契約と、正常、未検出、転送、例外の応答契約を利用例で確認できる。

C2-T2 経路引数、検索引数、要求情報をページ別の取得処理へ要求単位で渡し、そのJSON結果を既存SSRの描画入力へ接続する [R4,R11,R12,R13] <- C2-T1
done: 取得処理なしと非同期取得を扱え、直接アクセスごとに独立した入力から`render(input) -> { html, state }`を得られ、同時要求間で入力または状態が共有されず、取得処理の実装や要求情報が状態へ混入しない。

C2-T3 同じページ解決と取得処理から、直接アクセス用HTML応答とブラウザー遷移用JSON応答を返す接頭辞付きサブルーターを提供する [R3,R4,R11,R12,R13,R14] <- C2-T2
done: 接頭辞付きURLの直接アクセスと遷移用要求で同じ経路引数と検索引数が取得処理へ渡り、正常時は対応するページのHTML・初期状態・ブラウザー用生成物または取得結果を返し、未検出と転送は対応するHTTP応答になり、例外時は正常ページ用状態と内部情報を返さない。

C2-T4 サブルーター統合、既存SSRとの互換性、ファイル変更の反映、データ取得結果の分離を自動検証する [R1,R3,R4,R6,R11,R12,R13,R14] <- C2-T3
done: 直接アクセスと遷移用要求、接頭辞、取得処理なし、非同期取得、未検出、転送、例外、同時要求、検索引数変更、追加・削除・名前変更を自動試験で確認でき、既存の単一部品SSR利用が通る。

# C3

C3-T1 生成したクライアント経路表を使い、管理対象の同一生成元リンクと履歴操作からURLに対応するページデータを取得して画面を切り替える [R5,R9,R11,R12,R14] <- C2-T4
done: 動的経路のリンク、戻る・進む、検索引数だけの変更で文書全体を再読み込みせずに対応ページと取得結果を表示し、初回ハイドレーションではサーバーから渡された状態を使って取得処理を再実行しない。

C3-T2 画面切り替え時の破棄、連続遷移の競合抑止、対象外操作と失敗時の文書遷移を実装する [R9,R10,R13] <- C3-T1
done: 旧画面のイベント処理と副作用が破棄され、応答順が逆転しても最後に開始した遷移だけが表示を更新し、外部リンク、ダウンロード、別タブ操作、断片移動は標準動作を保ち、取得または描画の失敗時は対象URLへの文書遷移に移る。

C3-T3 直接アクセス、初回ハイドレーション、リンク遷移、履歴操作、失敗処理を一連の受入試験で検証する [R5,R9,R10,R11,R12,R13,R14] <- C3-T2
done: 直接アクセスとブラウザー遷移で同じページ、経路引数、検索引数、取得結果を表示し、初回の再取得がなく、対象外操作、旧画面の破棄、競合応答、未検出、転送、例外、通常の文書遷移への移行を自動試験で確認できる。
