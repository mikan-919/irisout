# Chunks
C1 [R1,R2,R5,R6,R7,R8,R14,K3,K4,A1,A2,A4,A5,A11] -> `page.jsx`のファイル構造から共通の経路定義とブラウザーだけで動く経路表を生成し、静的優先、衝突診断、末尾斜線、引数復号、未一致、再生成を同じ照合規則で検証できる
  boundary: OpenSpecの仕様変更、ファイル走査、順序と衝突の検査、環境非依存の照合処理までを含み、ページのコンパイルとHTTP応答は含めない。
C2 [R1,R3,R4,R6,R11,R12,R13,K1,K2,K3,K5,A1,A3,A5,A6,A9,A10,A11] <- C1 -> `irisout/hono`の接頭辞付きサブルーターが各ページを既存SSR生成物と要求単位の取得処理へ接続し、HTML・初期状態・遷移用JSON、未検出、転送、例外を同じ経路引数と検索引数から返せる
  boundary: ページ別のブラウザー生成物との対応、取得結果だけを搬送する応答形式、ファイル変更時の再生成を含む。HTML文書の外枠は利用側に残す。
C3 [R5,R9,R10,R11,R12,R13,R14,A4,A7,A8,A9,A10,A11] <- C2 -> 生成したクライアント経路表が管理対象リンクと履歴操作を捕捉し、取得結果で旧画面を破棄して次画面を表示しつつ、競合応答を無視し、対象外操作と失敗時にはブラウザーの文書遷移へ委ねる
  risk: 検索引数だけの変更も新しい遷移として扱い、初回ハイドレーションでは取得処理を呼ばず、連続遷移では最後に開始した遷移だけが画面を更新する。
