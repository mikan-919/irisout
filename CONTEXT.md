# irisout

JSXから「経験豊富なエンジニアが手書きしたVanilla JS」を生成するビルド時
コンパイラ。プロダクト全体の哲学は`CONCEPT.v2.md`、コンパイラ内部の構造は
`docs/architecture.md`を参照。ここでは**複数コンポーネント合成**
(2026-07-21のgrillingで決定、ADR-0014・change
`same-file-component-composition`で同一ファイル内に限り実装済み、
ROADMAP.md §4/§5)に関わる語彙のうち、実装コード上の概念(DeclId・
MarkerId・ゾーン等)とは別に、著者向けの語彙として固めておく必要が
あるものを記す。実装済みの制約の詳細はSTATUS.mdを参照。

## Language

**コンポーネント**:
ADR-0008のゾーン構造(変数ゾーン→`render()`→動きゾーン)を持ち、JSXタグ
として他のコンポーネントから参照される関数。呼び出し箇所でコンパイル時に
インライン化され、実行時にオブジェクト・インスタンスとして存在すること
はない(ADR-0004「コンポーネント境界はビルド後に消える」)。2026-07-21
時点の主な動機は、構造ユニット(リストアイテム/条件分岐ブランチ)の中身を
名前付きで切り出すこと ― アプリ全体に散らばる汎用的なUI合成(それには
context・モジュール共有stateが要る)はまだ対象外。
_Avoid_: インスタンス(実行時に存在する主体を連想させるため)

**props**:
コンポーネントの呼び出し箇所(JSX属性)から渡され、コンポーネント側は
分割代入で受け取る値。実行時のオブジェクトを経由せず、コンパイル時に
呼び出し箇所の式へ置換される(参照している箇所がそのまま呼び出し元の
式に書き換わる、という意味での「置換」)。ゆえにprops自身は追加の
リアクティブ機構を必要としない ― 置換先が signal 呼び出しなら既存の
依存グラフに、リストアイテムのitemパラメータならitem-level update に、
そのまま乗る。
_Avoid_: 引数(呼び出し規約の存在を連想させるため)、属性(host要素の
HTML属性と紛らわしいため)

**呼び出し箇所**:
コンポーネントをJSXタグとして参照している箇所。インライン化は呼び出し
箇所ごとに独立して行われる。同じコンポーネントが複数の呼び出し箇所から
参照されてもよい(2026-07-21時点でこれを1箇所に制限しないことを決定)。

**インライン化**:
コンポーネント参照を、そのコンポーネント本体のASTで置き換えるコンパイル
時の前処理。既存の`compileComponent`(render-tree走査)より**前**に行う
独立パスとして実装する ― 走査自体はコンポーネントという概念を知らない。

**ルートsignal**:
モジュールスコープの変数ゾーンで宣言されたsignal/derived。`update_<name>()`
もモジュールスコープに生成される(既存の実装、architecture.mdの用語を
踏襲)。

**ローカルsignal**:
コンポーネントの変数ゾーンで宣言されたsignal/derivedのうち、そのコン
ポーネントが構造ユニット(リストアイテム/条件分岐ブランチ)へインライン
化された結果、呼び出し箇所のfactoryクロージャへスコープされるもの。
`update_<name>()`相当はfactory内のローカル関数として生成され、モジュール
スコープには一切出ない ― JSのクロージャがインスタンスごとの分離をタダで
提供するため、ルートsignalと違い名前衝突の対象にもならない。
